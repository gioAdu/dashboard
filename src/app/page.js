import Link from "next/link";

export const metadata = {
  title: 'Home',
  description: 'Home page',
}

export default function Page() {
  return (
    <main>
      <div>START</div>
    </main>
  );
}
